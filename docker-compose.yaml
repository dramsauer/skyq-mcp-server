services:
  skyq-mcp:
    build: .
    env_file: .env
    # The server binds to SERVER_PORT inside the container, so publish the same
    # port on both sides — changing SERVER_PORT in .env "just works".
    ports:
      - "${SERVER_PORT:-8000}:${SERVER_PORT:-8000}"
    restart: unless-stopped
